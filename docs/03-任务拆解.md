# social-post · 任务拆解(Phase → Task)

> 每个 Task:先写测试 → 实现 → test/lint/type-check 过 → 更新进度文档 → 单 Task 单 commit + push。

## Phase 0 · 文档 + monorepo 脚手架
- [ ] T0.1 4 份文档(PRD/技术/拆解/进度)
- [ ] T0.2 根 `pnpm-workspace.yaml` + 根 `package.json`(脚本编排)+ `.gitignore` + `.env.example`
- [ ] T0.3 三端空骨架目录占位(apps/web、apps/server、services/crawler 的 README + 最小 package.json / pyproject)

## Phase 1 · 数据层(Prisma + Neon)
- [ ] T1.1 apps/server 初始化 NestJS 骨架(main/app module,Vitest 配置)
- [ ] T1.2 Prisma schema(Platform/Account/Post)+ `prisma generate`
- [ ] T1.3 连 Neon:`DATABASE_URL`,`prisma migrate dev` 建表
- [ ] T1.4 seed 脚本:插入 platform `instagram`
- [ ] T1.5 `PrismaModule`(NestJS) + 单测(service 能查到 seed 平台)

## Phase 2 · Python 爬虫服务(Scrapling + FastAPI)
- [ ] T2.1 pyproject/requirements + FastAPI 骨架 + `/health`
- [ ] T2.2 统一模型 `ProfileResult`/`PostItem`(pydantic)
- [ ] T2.3 IG `web_profile_info` 响应解析器(纯函数,输入 JSON → ProfileResult)+ pytest(fixture)
- [ ] T2.4 Scrapling fetch 封装(`InstagramCrawler.fetch_profile`),Bearer 鉴权中间件
- [ ] T2.5 `POST /crawl/{platform}` 路由 + 平台注册表;pytest(mock fetch)
- [ ] T2.6 Dockerfile + 本地 run 说明

## Phase 3 · NestJS 后端 API
- [ ] T3.1 `PlatformModule`:GET /api/platforms(+ 单测)
- [ ] T3.2 `AccountModule`:list/create/delete(+ 单测,unique 冲突处理)
- [ ] T3.3 `CrawlerModule`:HTTP client 调 crawler(+ 单测,mock http)
- [ ] T3.4 `PostModule` + 抓取编排:POST /api/accounts/:id/crawl(调 crawler → upsert account 画像 + posts)+ GET posts 分页(+ 单测)
- [ ] T3.5 全局:CORS、`/api` 前缀、异常过滤、校验管道

## Phase 4 · 前端(umi + tailwind + antd v6)
- [ ] T4.1 umi 初始化 + antd v6 + tailwind(关 preflight)+ 布局
- [ ] T4.2 API 层(request 封装 + 类型)
- [ ] T4.3 平台切换 + 账户管理(列表/新增/删除)
- [ ] T4.4 帖子表格(封面缩略图/文案/类型/点赞/评论/时间/链接 + 分页)+ 「抓取」按钮联动
- [ ] T4.5 账户画像卡片(头像/bio/粉丝等)
- [ ] T4.6 组件单测(Vitest + testing-library,核心表格渲染)

## Phase 5 · 部署配置
- [ ] T5.1 apps/web `vercel.json` + 环境变量文档
- [ ] T5.2 apps/server `vercel.json` + `api/index.ts` serverless 引导 + Prisma build 钩子
- [ ] T5.3 crawler Dockerfile 完善 + Railway/Render 配置 + 部署 README
- [ ] T5.4 三端本地联调跑通一次(真实/半真实)
- [ ] T5.5 根 README:本地启动 + 部署步骤 + 环境变量总表

## 里程碑

- **M1**(Phase 0-1):骨架 + 数据层可跑,能查到 seed 平台。
- **M2**(Phase 2):crawler 能对真实公开 IG 号返回结构化帖子。
- **M3**(Phase 3):server 端到端 `crawl` 落库成功。
- **M4**(Phase 4):前端点一下就能看到帖子表格。
- **M5**(Phase 5):Vercel 上线 + crawler 独立部署。
