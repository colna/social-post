# 离线测试依赖:不含 scrapling(fetcher 延迟 import,测试用 monkeypatch 替换)。
# 用法: pip install -r requirements-dev.txt && python -m pytest -q
fastapi>=0.110,<1.0
pydantic>=2.5,<3.0
httpx>=0.27
pytest>=8.0
# 仅 Python <3.11 需要(见 requirements.txt 说明)
eval_type_backport>=0.2; python_version < "3.11"
